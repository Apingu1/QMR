name: QMR source extraction
on:
  push:
    branches: [main]
permissions:
  contents: write
jobs:
  extract:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: pip install python-pptx
      - run: python tools/extract_qmr.py
      - run: python tools/inspect_qmr_reference.py
      - name: Commit extracted source text
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add analysis/extracted analysis/reference_structure.json
          git commit -m "Extract QMR source data" || exit 0
          git push
